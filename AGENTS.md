# AGENTS.md - petites-nuits

When working in this repository, follow the workspace-level instructions in `../AGENTS.md` when available, then read this app's `CLAUDE.md` for app-specific Android architecture, build, release, and gotcha details.

This app is its own git repository. Run `git` and Gradle commands from this directory, not from `/Users/benjamin/Documents/Apps`.

If working inside `ios/`, also read `ios/CLAUDE.md`.

